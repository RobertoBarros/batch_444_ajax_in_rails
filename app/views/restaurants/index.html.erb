<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <%= restaurant.name %>
      <%= link_to "Show", restaurant_path(restaurant)%>
      <%= link_to "Delete", restaurant, method: :delete, remote: true, data: {confirm: 'are you sure?'} %>
    </li>
  <% end %>
</ul>